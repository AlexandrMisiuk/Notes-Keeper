import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
  notesBox: {
    flexGrow: 0,
    width: "100%",
    display: "flex",
    flexFlow: "row wrap"
  }
}));
